
# Custom aliases:
```bash
# which show alias
alias which='alias | /usr/local/bin/which --tty-only --read-alias --show-dot --show-tilde'

alias ll='ls -l'

# linux
alias ls='ls --color=auto'

# mac ls only
alias ls='ls -G'
```
